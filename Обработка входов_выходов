# Обработка аналоговых сигналов AI
Out := N * (HL - LL) / 27648 + LL; // Out - выходное значение (Real); N - входное значение аналогового входа (Int); HL - верхняя граница выходного значения (Real); LL - нижняя граница выходного значения (Real);

# Обработка аналоговых выходов AO
Out := REAL_TO_INT ((N - L) / (H -L) * (Hi_Code - Low_Code) + Low_Code); // Out - выходное значение (Int); N - входное значение (Real); H - верхний диапазон (Real); L - нижний диапаон (Real); Low_Сode - нижний диапазон выходного значения (Real); Hi_Сode - верхний диапазон выходного значения (Real);

#Обработка аналоговых сигналов AI-Ni
